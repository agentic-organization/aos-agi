# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Secrets
.env
*.key
*.pem
secrets/

# Large / transient
*.log
logs/*.log
data/raw/**/*.tmp
data/derived/embeddings/*.bin
data/derived/embeddings/*.npy

# Search layer — build artifacts are regenerated by tools/search/build.sh
# Embeddings are produced separately on a bigger machine; neither is committed.
data/search/chunks.jsonl
data/search/bm25-index.json
data/search/embeddings.bin
data/search/embeddings.meta.json

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
